import React from "react";

const Slider = () => {
  return (
    <div
      style={{
        height: "15rem",
        display: "grid",
        placeItems: "center",
        content: "Slider",
        backgroundColor: "red",
      }}
    >
      Slider
    </div>
  );
};

export default Slider;
